/** @type {import('next').NextConfig} */
const nextConfig = {
  // If you were using static export, disable it to avoid useSearchParams suspense errors.
  // output: "export",
};

export default nextConfig;
